<ul>
    <li><strong>{{ cliente.nombre }}</strong></li>
    <li><strong>Teléfono</strong>: {{ cliente.telefono }}</li>
    {% if cliente.provincia %}
    <li><strong>Provincia: </strong>{{ cliente.provincia.nombre }}</li>
    {% endif %}
    <li><strong>Coche</strong>: {{ cliente.coche }}</li>
</ul>
